---
layout: base.njk
---
<h1>{{ site.name }}</h1>
<p>{{ site.bio }}</p>
<p>
  {%- for link in site.links %}
  <a href="{{ link.url }}">{{ link.label }}</a>{% if not loop.last %} /{% endif %}
  {%- endfor %}
</p>
