---
interface Props {
  experiment: {
    title: string;
    slug: string;
    description: string;
    date: string;
    tags: string[];
    type: string;
    link: string | null;
    thumbnail: string | null;
  };
}

const { experiment } = Astro.props;
const href = experiment.link || `/experiments/${experiment.slug}`;
const isExternal = !!experiment.link;
---

<a
  href={href}
  target={isExternal ? "_blank" : undefined}
  rel={isExternal ? "noopener noreferrer" : undefined}
  class="group block"
>
  <article class="overflow-hidden rounded-xl border border-border bg-card-bg transition-all hover:border-btn-secondary-hover-border hover:bg-card-hover-bg">
    {experiment.thumbnail && (
      <div class="relative aspect-video overflow-hidden">
        <img
          src={experiment.thumbnail}
          alt={experiment.title}
          class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
          loading="lazy"
        />
      </div>
    )}
    <div class="p-5">
      <div class="flex items-center gap-2">
        <span class="rounded-full bg-tag-bg px-2.5 py-0.5 font-mono text-xs text-tag-text">
          {experiment.type}
        </span>
        <time class="font-mono text-xs text-muted">
          {experiment.date}
        </time>
      </div>
      <h3 class="mt-3 text-lg font-semibold tracking-tight text-foreground transition-colors">
        {experiment.title}
      </h3>
      <p class="mt-1.5 line-clamp-2 text-sm leading-relaxed text-muted">
        {experiment.description}
      </p>
      {experiment.tags.length > 0 && (
        <div class="mt-3 flex flex-wrap gap-1.5">
          {experiment.tags.map((tag: string) => (
            <span class="rounded-full border border-border px-2 py-0.5 text-xs text-muted">
              {tag}
            </span>
          ))}
        </div>
      )}
    </div>
  </article>
</a>
